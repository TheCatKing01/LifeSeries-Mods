{
	"schemaVersion": 1,
	"id": "${id}",
	"name": "${name}",
	"version": "${version}",
	"authors": ["${authors}"],
	"contributors": ["${contributors}"],
	"contact": {
		"sources": "${sources_url}",
		"issues": "${issues_url}",
		"homepage": "${homepage_url}"
	},
	"description": "${description}",
	"icon": "icon.png",
	"license": "${license}",
	"environment": "*",
	"accessWidener": "aw/${sc_version}.accesswidener",
	"entrypoints": {
		"main": [
			"${group}.platform.fabric.FabricEntrypoint"
		],
		"client": [
			"${group}.client.platform.fabric.FabricClientEntrypoint"
		],
		"voicechat": [
			"net.mat0u5.lifeseries.compatibilities.voicechat.VoicechatMain"
		]
	},
	"mixins": [
		"${id}.mixins.json"
	],
	"breaks": {
		"pastlife": "*"
	},
	"depends": {}
}
